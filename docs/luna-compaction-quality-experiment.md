# Luna 压缩质量：三组实验

每组为自造、答案预先确定的档案，核心事实分散在开头、中间与结尾，并插入260条例行记录。模型固定 gpt-5.6-luna / low，compact_threshold=4000。每组一次压缩请求、一次仅携带最新压缩项及后续项的核对请求，共6次真实请求。没有向核对请求提供预期答案。

压缩项是加密内容，下列摘要是模型从压缩状态重新生成的文本，不是压缩项的解密结果。

## 项目变更

- 原请求输入：12710 tokens；核对请求输入：12613 tokens。
- 原输入数组：52282 bytes；保留的压缩状态：10488 bytes。
- 返回压缩项：2。HTTP状态：200/200。

重新生成的摘要：

> 项目 ORBIT-618 负责人为林岚，最新预算为7300元，截止日期为2026-11-23，文件名为 harbor.svg；发布前仍待字体授权，客户电话号码未提供。

| 字段 | 预期 | 实际 |
|---|---|---|
| project | ORBIT-618 | ORBIT-618 |
| owner | 林岚 | 林岚 |
| budget | 7300 | 7300 |
| deadline | 2026-11-23 | 2026-11-23 |
| artifact | harbor.svg | harbor.svg |
| forbidden | 禁止覆盖原稿 | 覆盖原稿 |
| pending | 等待字体授权 | 等待字体授权 |
| unknown | 未提供 | 未提供 |

## 故障调查

- 原请求输入：12680 tokens；核对请求输入：12639 tokens。
- 原输入数组：52316 bytes；保留的压缩状态：10872 bytes。
- 返回压缩项：2。HTTP状态：200/200。

重新生成的摘要：

> INC-842为导出后透明度丢失问题，根因为预乘透明度重复转换；修复后37项测试通过，但Safari尚未验证，部署日期未确定。

| 字段 | 预期 | 实际 |
|---|---|---|
| incident | INC-842 | INC-842 |
| symptom | 导出后透明度丢失 | 导出后透明度丢失 |
| cause | 预乘透明度重复转换 | 预乘透明度重复转换 |
| rejected | 不是缓存损坏 | 不是缓存损坏 |
| fix | 仅在最终编码前转换一次 | 仅在最终编码前转换一次，不禁用透明背景 |
| tests | 37 | 37 |
| pending | 尚未验证Safari | Safari验证 |
| unknown | 未提供 | 未提供 |

## 故事与关系

- 原请求输入：12453 tokens；核对请求输入：12652 tokens。
- 原输入数组：52265 bytes；保留的压缩状态：10660 bytes。
- 返回压缩项：2。HTTP状态：200/200。

重新生成的摘要：

> 档案显示，雾港的铜钥匙由许澄保管，周禾曾借用但并非所有人；密封箱位于旧钟楼第三层，须先送信再开门，禁止午夜敲钟。

| 字段 | 预期 | 实际 |
|---|---|---|
| city | 雾港 | 雾港 |
| keeper | 许澄 | 许澄 |
| key | 铜钥匙 | 铜 |
| location | 旧钟楼第三层 | 旧钟楼第三层 |
| notowner | 周禾 | 周禾 |
| sequence | 先送信再开门 | 先送信再开门 |
| constraint | 不能在午夜敲钟 | 禁止在午夜敲钟 |
| unknown | 未提供 | 未提供 |

## 人工核对与限制

24个定向核对点均在语义上吻合。逐字匹配为19/24：覆盖原稿出现在“禁止事项”字段，Safari验证出现在“待验收”字段，铜回答材质问题，禁止与不能同义，修复项补充“不禁用透明背景”也有原文依据。没有把未知电话、部署日期或年龄编造出来。

短摘要本身并不完整：项目摘要漏掉禁止覆盖原稿，故障摘要漏掉具体修复方式和已排除假设，但字段核对能找回。因此不能把摘要文本当成压缩状态的完整呈现。

传输数据约减少79%–80%，报告输入token未显著下降，不能称为额度节省或有效上下文扩大。档案以稀疏关键事实和例行记录构成，不代表高密度长篇、真实多轮工具调用或超长会话表现。本次没有DSH压缩对照组，不能据此断言优于DSH。

## 原样复述对照

同一份约1.24万token项目档案，使用Luna low。实验组只带最新compaction及后续项，控制组携带未压缩原文；两组使用相同复述指令，要求保留项目核心段落的原始措辞与历史修改，省略例行记录。不是DSH原生压缩的对照，也不是要求输出服务端内部摘要。

两组输出完全相同，忽略空白后均与原文核心段落完全一致。旧预算6200、新预算7300、原日期2026-11-18、新日期2026-11-23、禁止覆盖原稿和待字体授权均原样保留。

> 项目 ORBIT-618 的负责人是林岚。初始预算 6200 元，截止日期原定 2026-11-18。最终文件名 harbor.svg。禁止覆盖原稿。
> 
> 变更记录：预算增加到 7300 元，日期延后至 2026-11-23。其他字段不变。
> 
> 发布前仍在等待字体授权。客户电话号码从未提供。

实验组输入12569 tokens，控制组输入12363 tokens。可以验证本样本核心原文可恢复，不能将复述认作内部压缩表示，也不能推断全部档案无损或优于DSH。证据：.artifacts/maintenance-pass/compression-verbatim-comparison.json。
